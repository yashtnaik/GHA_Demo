name: shell-script-job

on:
  push:
    branches:
    - main

jobs:
  execute-shell:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Run a one-line script
      run: |
        chmod +x tejas.sh
        ./tejas.sh
